const facebookMethod = require('./facebook');
const googleMethod = require('./google');
const githubMethod = require('./github');

module.exports = {
  facebookMethod,
  googleMethod,
  githubMethod,
}